<%= form_with(model: [:admin, page]) do |form| %>
  <%= render(
    partial: form,
    locals: { attribute: attribute },
  ) %>
  <%= form.button(type: :button, "data-action": "cancel") do %>
    <%= translate("helpers.submit.page.cancel") %>
  <% end %>
<% end %>
